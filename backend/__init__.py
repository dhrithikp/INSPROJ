"""Backend package initializer - makes `backend` importable as a package.

This file is intentionally empty; its presence allows running:
  uvicorn backend.main:app
from the project root directory.
"""
